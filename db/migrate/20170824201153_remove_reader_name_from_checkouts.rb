class RemoveReaderNameFromCheckouts < ActiveRecord::Migration[5.1]
  def change
    #This migration will not do anything, the setps in the challenge are unclear and a reader name was not originally added to out checkouts table if it was this migration would remove that column.
  end
end
